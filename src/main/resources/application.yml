debug: true

server:
  port: 8091

spring:
  application:
    name: lnh-api

management:
  endpoints:
    web:
      exposure:
        include: '*'

lnh.example:
  a: 'a form application.yml'
  b: 'b form application.yml'
